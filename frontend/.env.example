# CarbonTrail frontend environment
# Copy to .env.local and adjust as needed.

# Backend base URL that Next.js rewrites /api/* to (see next.config.ts).
#   Local dev (backend on host):  http://localhost:8902
#   docker compose (by service):  http://backend:8902
NEXT_PUBLIC_API_URL=http://localhost:8902
